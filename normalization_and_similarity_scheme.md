# 人脸识别特征归一化与相似度计算方案

本文档基于项目代码（`nn_functions.cpp` 和 `main.cpp`）解析了人脸识别系统中特征向量的归一化处理及相似度计算的具体实现方案。

## 1. 特征向量生成 (Feature Generation)

系统采用了一种**测试时增强 (Test-Time Augmentation, TTA)** 的策略来生成最终的特征向量，以提高识别的鲁棒性。

*   **双路推理**：
    1.  **原始图像**：将裁剪好的 112x112 人脸图像输入神经网络，提取出一个 128 维的特征向量。
    2.  **镜像图像**：将同一张人脸图像进行**水平翻转**，再次输入神经网络，提取出另一个 128 维的特征向量。
*   **特征拼接**：
    将上述两个 128 维向量直接拼接，形成一个 **256 维** 的组合特征向量。
    > **代码对应**：
    > ```cpp
    > fr_run(face256, image112_left);      // 前128维
    > fr_run(face256+128, image112_right); // 后128维 (翻转图)
    > ```

## 2. 归一化处理流程 (Normalization)

在进行比对之前，原始的 256 维特征向量需要经过两步严格的标准化处理。

### 2.1 去均值 (Mean Subtraction / Centering)
*   **目的**：移除特征空间中的“共性”成分（即所有脸共有的平均特征），从而突出个体之间的差异性特征。
*   **方法**：从当前的特征向量中减去一个全局平均向量 (`face_mean`)。
*   **来源**：`face_mean` 是在底库注册阶段，统计所有已注册人脸特征计算得出的平均值。
*   **公式**：
    $$ v_{centered} = v_{raw} - v_{mean} $$

### 2.2 L2 归一化 (L2 Normalization)
*   **目的**：将特征向量转化为**单位向量**（Unit Vector）。消除向量模长（Magnitude）对相似度计算的影响，使得后续的点积计算直接等价于余弦相似度。
*   **方法**：
    1.  计算去均值后向量的欧几里得范数（模长）。
    2.  将向量的每个元素除以该模长。
*   **公式**：
    $$ v_{norm} = \frac{v_{centered}}{\|v_{centered}\|_2} $$
    其中 $\|v\|_2 = \sqrt{\sum v_i^2}$。
*   **代码对应**：
    ```cpp
    // 去均值 & 计算模长平方
    for(int i=0; i<256; i++){
        vec1[i] = (face256[i] - face_mean[i]); 
        total += vec1[i] * vec1[i];
    }
    // L2 归一化
    total = sqrt(total);
    for(int i=0; i<256; i++){
        vec1[i] = vec1[i] / total;
    }
    ```

## 3. 相似度计算方案 (Similarity Calculation)

系统使用**余弦相似度 (Cosine Similarity)** 作为衡量两个人脸特征是否属于同一人的标准。

### 3.1 计算原理
由于参与比较的两个向量（待识别向量 $A$ 和底库向量 $B$）都已完成了 L2 归一化（模长均为 1），它们的点积直接等于夹角的余弦值：

$$ \text{Similarity} = A \cdot B = \|A\| \|B\| \cos(\theta) = 1 \cdot 1 \cdot \cos(\theta) = \cos(\theta) $$

### 3.2 实现逻辑
1.  **点积运算**：
    遍历底库中的每一个特征向量，将其与待识别向量进行点积运算（对应元素相乘并累加）。
    ```cpp
    float total = 0;
    for(int j=0; j<256; j++)
        total += vec1[j] * vector_save[i][j];
    ```

2.  **阈值筛选**：
    设定了一个硬阈值 **0.4**。
    *   如果 $\text{Similarity} > 0.4$，则认为匹配成功。
    *   如果 $\text{Similarity} \le 0.4$，则认为匹配失败或非同一人。

3.  **最大值优选 (Top-1 Selection)**：
    在所有满足阈值条件的底库人脸中，选择相似度得分**最高**的那一个作为最终识别结果。

### 3.3 结果输出
函数 `veccmp` 返回一个混合数值 `ID + Score`（例如 `5.87`），调用者通过取整获得 ID，取小数部分获得置信度。
